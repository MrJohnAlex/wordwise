// import CityItem from "./CityItem";

export default function CityList(props) {
  console.log(props);

  return (
    <ul>
      {/* {cities.map((city) => (
        <CityItem city={city} key={city.id} />
      ))} */}
    </ul>
  );
}
